## Pre-Flight Gate Assessment

**Artifact:** /home/fanderman/projects/luigis-box/.agent_context/worktrees/session-1781106672-6469-2465b2ba685c/.agent_context/sessions/1781106672-6469-2465b2ba685c/fix1-out-of-scope-design.md
**Generator type:** sketch
**Round:** 1 of 1
**Verdict:** REQUEST-CHANGES (concerns)

### Flags (2)
- **[severity: medium]** Dual-write `llm_call_count` edits live only in narrative, not in the build manifest: the "Integration Points → Files to modify, in order" numbered list has three items (add `_out_of_scope_system_prompt`, add `_out_of_scope_reply_from_response`, rewrite `out_of_scope_deflect`) and stops there. The two helper-signature edits (`_deflection_update`, `_emit_deflection_classification`) appear ONLY under "Second-order chains §B". An implementer who builds the numbered list verbatim edits `out_of_scope_deflect` to call both helpers with `llm_call_count=1` but never adds the parameter to the helpers → at minimum a `TypeError: unexpected keyword argument`; worse, if they add the param to `_deflection_update` only (the more obvious one) and miss `_emit_deflection_classification`, the emitted turn-classification telemetry under-reports the call as 0 while state reports 1 — the exact silent divergence §B itself warns about. This is the precise failure success-criterion (b) asked to be scrutinised: the coupling is correctly *identified* in prose but is NOT lifted into the verbatim build list, so it is in-scope-but-skippable rather than unambiguously in-scope.
  evidence: [verified: conversational-search/src/conversational_search/agent/graph.py (_emit_deflection_classification)] line 3213 `llm_call_count=0` (hardcoded); [verified: conversational-search/src/conversational_search/agent/graph.py (_deflection_update)] line 3233 `"llm_call_count": 0` (hardcoded). Both premises of §B confirmed true on disk.
- **[severity: low]** Citation line-drift (advisory, name-recoverable): every `graph.py:NNN` anchor is stale by a uniform ~33–47 lines because the file grew since authoring — `_gift_advisor_turn1_system_prompt` cited 1850/actual 1897; `_gift_advisor_opener_from_response` 1863/1910; `_messages_with_system_prompt` 1914/1961; `_resolve_language_name` 2983/3030; `_emit_deflection_classification` 3166/3199; `_deflection_update` 3186/3223; `out_of_scope_deflect` 3270-3287/3317-3334; `support_deflect` 3232/3279; `unsafe_deflect` 3290/3337; U2 fallback string 3275-3279/3322-3326. Consumer: an implementer navigating by raw line number lands ~3–4 dozen lines above the target. Low because every anchor also names the symbol and grep-by-name recovers cleanly; no fix strictly required, but a one-line "navigate by symbol name, line numbers are approximate" note would harden the build.
  evidence: [verified: conversational-search/src/conversational_search/agent/graph.py (out_of_scope_deflect)] resolves at line 3317, sketch cites 3270-3287.

### Summary
The sketch is internally consistent and almost entirely buildable-as-written: the verbatim system prompt meets the contract (≤18 words, polite, no-apologies double-enforced, language-adaptive via `{language}`); the single `ainvoke` shape preserves `llm_call_count==1` (0→1, `1 <= max_llm_calls_per_turn_1`); the language-injection point is concrete and grounded (`_resolve_request_language` → `_resolve_language_name` → `_out_of_scope_system_prompt`, the same path `mode_dispatch` and `support_deflect` use). Risk (a) — the `astream`→`ainvoke+TAG_NOSTREAM` deviation — is **adequately resolved and non-blocking**: the deviation is well-justified (deflections render on the `deflection_text` custom channel, confirmed at custom_events.py:27/99), `TAG_NOSTREAM` is an established codebase idiom (graph.py:25, :2564), and the residual U1 (proxy grep) is confirmatory rather than load-bearing because the current static path never streamed — so `ainvoke` cannot regress the UI even if the proxy grep is inconclusive. Risk (b) — the dual-write coupling — has its premise **verified true on disk** (both helpers hardcode 0), and the sketch correctly names the under-report trap; the single concern is that the two helper edits are not promoted into the numbered build list, leaving a skippable instruction for a checklist-following implementer. One fix-round step (promote §B's two edits to the build manifest as an explicit step 4) closes it. Defect is internal to the artifact and fix-round-closable → concerns, not block.

Findings emission self-check: 2 flags, 0 annotation-paired, 2 consequence-named.
