## Pre-Flight Gate Assessment

**Artifact:** docs/v2-design/signature-cache-validation-freshness-report.md
**Generator type:** research
**Round:** 1 of 1
**Verdict:** REQUEST-CHANGES

### Flags (3)

- **[severity: high]** Hardcoded foreign session-id in the binding SSE-capture paths: `MISS_SSE`/`HIT_SSE` (report lines 51-52) are pinned to `.agent_context/sessions/1781106672-6469-2465b2ba685c/cache-validation-${LANG_CODE}-{miss,hit}.sse`. That literal session-id belongs to the gate/authoring session, NOT to the Subtask 10 or Subtask 11 consumer sessions that will copy this runbook verbatim. Defect site: report lines 51-52. Downstream consumer: Subtasks 10/11 researchers running the curl `> "$MISS_SSE"` redirect. Mis-action manifestation: the curl redirect writes SSE captures into a different session's directory (or a path absent in the consumer's worktree), so the python SHA-comparator at lines 119-151 reads stale/foreign or non-existent `.sse` files and either compares the wrong session's payload or fails on FileNotFoundError — a mis-asserted pass/fail. Fix: define these paths relative to the consumer's own session/run dir (e.g. derive from a `RUN_DIR` variable the consumer sets, or use a worktree-relative scratch path), and stop embedding any literal session-id. [should-be-issue]
  evidence: docs/v2-design/signature-cache-validation-freshness-report.md:51-52

- **[severity: medium]** `query_text` is a NULLable column populated only by current proxy code, and the entire MISS-force procedure keys exclusively on it — but the report never states the precondition that the running proxy must be a build that WRITES `query_text`. Defect site: report line 7 (table-columns claim) and the DELETE/SELECT predicates at lines 70 and 95, all keyed on `query_text`. Downstream consumer: Subtasks 10/11 running the `DELETE ... WHERE tracker_id=:'tracker' AND query_text=:'q'` force-MISS step. Mis-action manifestation: migration 0004 (verified) adds `query_text` as `nullable=True` with the rationale "old code emits no value; worker skips NULL-query rows" — so if the live proxy under test is a pre-0004-write build, cached rows carry `query_text IS NULL`, the DELETE matches zero rows, `rows_after_delete=0` reports a false-clean, and a STALE cached row survives. The consumer then issues the "MISS" request against a live HIT and mis-asserts freshness. Fix: add a one-line precondition stating the proxy build must populate `query_text` on write, and recommend a sanity check (e.g. confirm a freshly written row has non-NULL `query_text`) before trusting the `rows_after_delete=0` signal. [should-be-issue]
  evidence: conversational-search/conversational-proxy/alembic/versions/20260527040001_0004_v2_add_query_text.py (upgrade)

- **[severity: medium]** The python comparator asserts `miss[3] == hit[3]` over a fixed `keep` key set that includes `triggering_keyword` and `verbatim_query`, but several non-product-search rows that the report ALSO routes through this same MISS/HIT comparator (it presents one verbatim procedure for all rows) will populate those fields differently or as None, and the report does not scope the SHA/turn_classification equality assertion to the cacheable product-search rows (V2-V5) only. Defect site: report lines 100-151 (the "For rows with a DB row, issue the HIT ... assert miss[3]==hit[3]" block) vs the row-set scoping at lines 38-39. Downstream consumer: Subtasks 10/11 applying the comparator to any V-row that happens to produce a DB row. Mis-action manifestation: the report says V7-V20 are MISS-only "unless a cache row appears" (lines 24-36) — so a consumer that DOES get a row for, say, a takeover prompt will run this comparator; the assertion set is validated as correct ONLY for the product-search payload shape, and the report does not tell the consumer the equality check is the product-search HIT-replay proof, not a general per-row gate. Fix: explicitly scope the SHA/turn_classification equality assertion to the cacheable product-search rows (V2-V5), and state what a consumer should assert for a takeover row that unexpectedly produces a HIT row.
  evidence: docs/v2-design/signature-cache-validation-freshness-report.md:140-150

### Summary

The report's cache-key facts, eviction-predicate scope claim, MISS-only fingerprint-mismatch exceptions, and code-anchor citations all re-trace clean against source. It is blocked on three runbook-executability defects: a hardcoded foreign session-id in the SSE-capture paths (high), an unstated `query_text`-population precondition that can produce a false-clean MISS (medium), and an unscoped SHA-equality assertion applied to rows it was only validated for on product-search (medium). All three would make a copy-paste consumer write to a wrong path, mis-assert freshness, or apply a comparator out of its validated scope.

Findings emission self-check: 3 flags, 3 annotation-paired, 3 consequence-named.
